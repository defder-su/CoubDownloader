CMDDIR="$(cd "$(dirname "$0")" && pwd)"

$CMDDIR/save $1

SUBDIR="$1"
if [ -e "$SUBDIR" ]; then
    cd "$SUBDIR"
    ls *.html | cut -f 1 -d '.' | xargs -L1 $CMDDIR/save_recoubs_by_html
    cd ..
else
    exit 1
fi
